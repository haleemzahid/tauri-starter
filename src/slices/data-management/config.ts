import { Database } from 'lucide-react'
import type { NavItem } from '@/config/nav-items'

export const dataManagementConfig: NavItem = {
  label: 'Settings',
  path: '/settings',
  icon: Database,
}
