import { ReceiptText } from 'lucide-react'
import type { NavItem } from '@/config/nav-items'

export const orderEntryNavItem: NavItem = {
  label: 'Order Entry',
  path: '/order-entry',
  icon: ReceiptText,
}
